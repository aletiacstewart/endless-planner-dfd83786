# Fix planner-style controls on mobile

## What will change
- Replace the single scrolling row of planner-style buttons with a compact wrapping layout on phones, so Title, Subtitle, Body, Background, Cards, Accent, Spacing, and Clear are all visible.
- Keep the controls compact and touch-friendly without widening the planner page.
- Make opened font, colour, paper, and spacing options fit within the phone width without clipping.
- Keep the preview and Apply button directly below the controls and fully inside the planner-style card.
- Preserve the existing desktop layout and planner-wide Apply behavior.

## Verification
- Check the planner's second page at the current phone size and confirm every styling button is visible without sideways scrolling.
- Open each styling option and confirm its choices remain usable and contained.
- Confirm the preview updates and the Apply dialog opens correctly on mobile.
- Recheck desktop to ensure the existing layout remains unchanged.
